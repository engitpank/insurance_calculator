**Контекст задачи (краткое описание) **
Отрефакторить тесты для реализации метода TravelCalculatePremiumService.calculatePremium():
написать отдельный тестовый метод на каждое поле ответа.

**Что было сделано и как? (краткое описание)**
Сначала просто создал тесты для каждого из поля. Затем вынес инициализацию сервисного класса и тела запроса в метод setUp.

**Сколько потратил времени на выполнение задачи?**
~ 20 минут - время потратил на размышления: стоит ли на шаге arrange создавать expectedLastName или же сразу в assert
вызывать конструктор. Принял решение в пользу первого, так как оно видится более читаемым.

**Заметки, идеи, предложения в свободной форме**